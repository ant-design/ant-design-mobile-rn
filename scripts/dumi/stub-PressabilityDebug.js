'use strict';

/**
 * Stub for react-native-gesture-handler on doc site (web).
 * It imports react-native/Libraries/Pressability/PressabilityDebug;
 * we alias that to this file so the docs build does not pull in real RN.
 */
function PressabilityDebugView() {
  return null;
}

module.exports = { PressabilityDebugView };
