'use strict';

/**
 * Stub for react-native-gesture-handler on doc site (web).
 * codegenNativeComponent 在文档站无原生实现，导出空组件即可。
 */
function codegenNativeComponent() {
  return function StubNativeComponent() {
    return null;
  };
}

module.exports = codegenNativeComponent;
